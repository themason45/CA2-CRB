package support.menu;

/**
 * {@link BaseMenuOption} which accesses a method of a model
 */
public class ModelOption extends BaseMenuOption {
    public ModelOption(String title) {
        super(title);
    }
}
